apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: team02
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: 093322228889.dkr.ecr.ap-northeast-2.amazonaws.com/team02-user-service:bdcae142cac45fa349fecdb3bdf11e8ed3952347
        ports:
        - containerPort: 8082
        envFrom:
          - configMapRef:
              name: spring-config
          - secretRef:
              name: app-secrets
        resources:
          requests:
            cpu: "250m"
            memory: "512Mi"
          limits:
            cpu: "500m"
            memory: "1024Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: team02
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
  - port: 8082
    targetPort: 8082
    protocol: TCP


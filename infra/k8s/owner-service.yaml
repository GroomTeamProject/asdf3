apiVersion: apps/v1
kind: Deployment
metadata:
  name: owner-service
  namespace: team02
spec:
  replicas: 1
  selector:
    matchLabels:
      app: owner-service
  template:
    metadata:
      labels:
        app: owner-service
    spec:
      containers:
      - name: owner-service
        image: 093322228889.dkr.ecr.ap-northeast-2.amazonaws.com/team02-owner-service:3dce15eb551fbe261cca91dab3a3c0c236c2d95b
        ports:
        - containerPort: 8081
        envFrom:
          - configMapRef:
              name: spring-config
          - secretRef:
              name: app-secrets
        resources:
          requests:
            cpu: "250m"
            memory: "512Mi"
          limits:
            cpu: "500m"
            memory: "1024Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: owner-service
  namespace: team02
spec:
  type: ClusterIP
  selector:
    app: owner-service
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP


apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: team02
  labels:
    Service: notification-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
        Service: notification-service
    spec:
      containers:
      - name: notification-service
        image: 093322228889.dkr.ecr.ap-northeast-2.amazonaws.com/team02-notification-service:latest
        ports:
        - containerPort: 8088
        envFrom:
          - configMapRef:
              name: spring-config
          - secretRef:
              name: app-secrets
        resources:
          requests:
            cpu: "250m"
            memory: "512Mi"
          limits:
            cpu: "500m"
            memory: "1024Mi"

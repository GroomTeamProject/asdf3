apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: team02
  labels:
    Service: order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
        Service: order-service
    spec:
      containers:
      - name: order-service
        image: 093322228889.dkr.ecr.ap-northeast-2.amazonaws.com/team02-order-service:latest
        ports:
        - containerPort: 8085
        envFrom:
          - configMapRef:
              name: spring-config
          - secretRef:
              name: app-secrets
        resources:
          requests:
            cpu: "250m"
            memory: "512Mi"
          limits:
            cpu: "500m"
            memory: "1024Mi"
